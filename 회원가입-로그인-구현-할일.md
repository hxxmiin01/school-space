# 회원가입·로그인 기능 구현 할 일

> 스쿨스페이스의 이메일 회원가입과 로그인을 단계별로 완성하기 위한 체크리스트입니다.
>
> 한 번에 하나씩 진행하고, 각 단계가 끝나면 화면에서 직접 확인합니다.

## 현재 상태

- [x] 로그인 화면이 만들어져 있음
- [x] 회원가입 화면이 만들어져 있음
- [x] 이메일과 비밀번호로 Supabase Auth(로그인 서비스를 맡는 기능)에 가입 요청
- [x] 이메일과 비밀번호로 로그인 요청
- [x] 인증메일 다시 보내기 기능
- [x] 회원가입 때 이름과 학급을 `profiles` 표에 저장하려는 코드
- [x] 로그인 후 `profiles.role`을 읽어 관리자 메뉴 표시 여부 확인
- [x] 로그인 상태 초기화 코드 정리
- [x] 로그인 관련 파일 ESLint 검사 통과
- [ ] 실제 이메일로 회원가입부터 로그인까지 확인
- [ ] 인증번호 입력 방식 구현 여부 결정

## 1단계: Supabase 연결 확인

Supabase(회원정보를 보관하고 인증메일을 보내주는 서비스)가 앱과 연결되어 있는지 확인합니다.

- [x] `school-space-app/.env`에 `VITE_SUPABASE_URL`이 있는지 확인
- [x] `school-space-app/.env`에 `VITE_SUPABASE_ANON_KEY`가 있는지 확인
- [ ] 두 값이 같은 Supabase 프로젝트의 값인지 확인
- [x] Supabase 대시보드에서 Email 로그인 기능이 켜져 있는지 확인
- [x] 로컬 개발 주소가 Site URL 또는 Redirect URL(인증 후 앱으로 돌아올 주소)에 등록되어 있는지 확인

> 공개 키는 앱에서 사용할 수 있지만, Supabase의 `service_role` 키는 절대로 프론트엔드에 넣지 않습니다.

## 2단계: 현재 이메일 링크 방식 확인

현재 앱은 이메일에 있는 링크를 누르는 방식으로 인증합니다.

- [ ] 테스트용 이메일 주소 준비
- [ ] 회원가입 화면에서 이름 입력
- [ ] 이메일 입력
- [ ] 비밀번호 6자리 이상 입력
- [ ] 학년과 반 선택
- [ ] `회원가입 완료` 버튼 클릭
- [ ] 인증메일이 도착했는지 확인
- [ ] 스팸메일함도 확인
- [ ] 이메일 안의 인증 링크 클릭
- [ ] 앱으로 돌아와 로그인
- [ ] 로그인 후 공간 현황 화면이 보이는지 확인

### 성공 기준

```text
회원가입
→ 인증메일 도착
→ 이메일 속 링크 클릭
→ 로그인 성공
→ 공간 현황 화면 표시
```

## 3단계: `profiles` 표 확인

`profiles` 표는 로그인 계정에 이름, 학급, 권한을 붙여 두는 표입니다.

- [ ] `profiles` 표가 만들어져 있는지 확인
- [ ] `id` 컬럼이 있는지 확인
- [ ] `id`가 Supabase Auth 사용자 ID와 연결되는지 확인
- [ ] `role` 컬럼이 있는지 확인
- [ ] `name` 컬럼이 있는지 확인
- [ ] `class_name` 컬럼이 있는지 확인
- [ ] `created_at` 컬럼이 필요한 경우 자동으로 저장되는지 확인
- [ ] 회원가입 후 `name`이 올바르게 저장되는지 확인
- [ ] 회원가입 후 `class_name`이 예를 들어 `1학년 3반`으로 저장되는지 확인
- [ ] 새로고침 후 마이페이지에 이름과 학급이 보이는지 확인

## 4단계: 보안 규칙 설정

RLS(행 단위 보안)는 사용자가 다른 사람의 정보를 함부로 보지 못하게 하는 잠금장치입니다.

- [ ] `profiles` 표에서 RLS 켜기
- [ ] 로그인한 사용자가 자신의 프로필을 읽을 수 있게 설정
- [ ] 로그인한 사용자가 자신의 프로필을 만들 수 있게 설정
- [ ] 로그인한 사용자가 자신의 학급 정보를 수정할 수 있게 설정
- [ ] 일반 사용자가 자신의 `role`을 `admin`으로 바꾸지 못하게 설정
- [ ] 다른 사용자의 프로필을 읽을 수 없는지 확인
- [ ] `reservations` 표에도 필요한 RLS 규칙이 있는지 확인
- [ ] `penalties` 표에도 필요한 RLS 규칙이 있는지 확인

## 5단계: 회원가입 오류 처리 확인

회원가입은 Auth 계정 만들기와 `profiles` 정보 저장, 두 가지 작업으로 이루어집니다.

- [ ] 잘못된 이메일 형식을 입력했을 때 안내문이 보이는지 확인
- [ ] 6자리 미만 비밀번호가 막히는지 확인
- [ ] 이름을 비웠을 때 안내문이 보이는지 확인
- [ ] 학년이나 반을 선택하지 않았을 때 안내문이 보이는지 확인
- [ ] 이미 가입한 이메일을 다시 사용했을 때 안내문이 보이는지 확인
- [ ] Supabase 연결이 끊겼을 때 이해하기 쉬운 안내문이 보이는지 확인
- [ ] `profiles` 저장에 실패했을 때 사용자가 다음에 무엇을 해야 하는지 알 수 있는지 확인

> Auth 계정은 만들어졌지만 `profiles` 저장에 실패하는 경우가 있을 수 있습니다. 처음에는 명확한 오류 안내를 제공하고, 자동 복구는 나중에 별도 서버 기능으로 진행합니다.

## 6단계: 로그인과 세션 확인

세션(로그인 상태를 기억하는 정보)이 새로고침 뒤에도 유지되는지 확인합니다.

- [ ] 올바른 이메일과 비밀번호로 로그인
- [ ] 틀린 비밀번호로 로그인했을 때 오류 안내
- [ ] 존재하지 않는 이메일로 로그인했을 때 오류 안내
- [ ] 이메일 인증 전에는 로그인할 수 없는지 확인
- [ ] 로그인 후 새로고침
- [ ] 새로고침 뒤에도 로그인 상태가 유지되는지 확인
- [ ] 로그아웃 후 로그인 화면으로 돌아가는지 확인
- [ ] 일반 학생 계정에 관리자 메뉴가 보이지 않는지 확인
- [ ] 관리자 계정에만 관리자 메뉴가 보이는지 확인

## 7단계: 관리자 권한 확인

- [ ] 일반 학생의 `role`이 `student`인지 확인
- [ ] 관리자의 `role`이 `admin`인지 확인
- [ ] 일반 학생이 `/admin` 주소로 직접 들어가도 홈으로 이동하는지 확인
- [ ] 관리자가 담당자 관리 화면을 볼 수 있는지 확인
- [ ] 일반 사용자가 다른 사람의 권한을 바꿀 수 없는지 확인

## 8단계: 인증번호 방식으로 바꿀지 결정

현재는 인증메일의 링크를 누르는 방식입니다. 사용자가 원하는 것이 이메일 속 숫자 코드를 앱에 입력하는 방식인지 결정합니다.

- [ ] 이메일 링크 방식으로 회원가입과 로그인이 먼저 성공했는지 확인
- [ ] 숫자 인증번호 입력 방식이 꼭 필요한지 결정
- [ ] 인증번호 방식으로 결정했다면 Supabase 이메일 템플릿을 확인
- [ ] 이메일 안에 인증번호가 표시되도록 템플릿 설정
- [ ] 회원가입 완료 뒤 인증번호 입력 화면 추가
- [ ] 인증번호 입력칸 추가
- [ ] 인증 확인 버튼 추가
- [ ] 인증메일 다시 보내기 버튼 추가
- [ ] 틀린 인증번호 안내 추가
- [ ] 만료된 인증번호 안내 추가
- [ ] 인증번호 확인에 `verifyOtp()` 사용
- [ ] 인증 성공 뒤 로그인 화면 또는 앱 화면으로 이동

### 인증번호 방식의 목표 흐름

```text
회원가입
→ 인증메일 도착
→ 이메일에서 인증번호 확인
→ 앱에 인증번호 입력
→ 인증 성공
→ 로그인
```

## 9단계: 최종 테스트

새로운 테스트 이메일을 사용해 처음부터 끝까지 확인합니다.

- [ ] 정상 회원가입
- [ ] 인증메일 수신
- [ ] 인증 링크 또는 인증번호 확인
- [ ] 정상 로그인
- [ ] 새로고침 후 로그인 유지
- [ ] 로그아웃
- [ ] 다시 로그인
- [ ] 중복 이메일 처리
- [ ] 잘못된 비밀번호 처리
- [ ] 인증메일 재전송
- [ ] 마이페이지에서 이름과 학급 확인
- [ ] 예약 화면에서 학급 정보가 사용되는지 확인
- [ ] 일반 학생과 관리자 메뉴 구분 확인
- [ ] 다른 사용자의 정보가 보이지 않는지 확인

## 코드 검사 명령

PowerShell에서 `school-space-app` 폴더로 이동한 뒤 실행합니다.

```powershell
Set-Location school-space-app
npm run build
npx eslint src/pages/LoginPage.jsx src/App.jsx src/supabaseClient.js
```

전체 ESLint 검사도 실행할 수 있습니다.

```powershell
npm run lint
```

> 전체 검사에서 Azure Functions 테스트 파일의 Jest(Node 테스트 도구) 전역 변수 오류가 나오면, 회원가입 코드 오류와 분리해서 기록합니다.

## 구현할 때 지킬 순서

1. 한 번에 한 단계만 진행합니다.
2. 코드를 바꾼 뒤 관련 파일 검사를 먼저 실행합니다.
3. 브라우저에서 사용자가 직접 확인합니다.
4. 문제가 생기면 `기대한 결과`와 `실제로 일어난 결과`를 나눠 적습니다.
5. 비밀번호는 `profiles` 표에 저장하지 않습니다.
6. `SUPABASE_SERVICE_ROLE_KEY`는 프론트엔드에 넣지 않습니다.
7. 인증번호 방식은 현재 링크 방식이 성공한 뒤 추가합니다.
